import 'package:flutter/material.dart';

class Entrance extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return  Scaffold(
      body: Center(
        child: Text(
          "Karyam",
          style: TextStyle(fontSize: 55, fontFamily: 'FontField',color: Colors.deepPurple.shade200),
        ),
      ),
    );
  }
}
